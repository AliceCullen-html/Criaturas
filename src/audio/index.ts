// Som do jogo. Hoje, a música de fundo; amanhã, a voz sintetizada das
// criaturas (etapa E) — que também não é assunto do renderer nem da UI.

export { createMusic } from './music';
export type { Music } from './music';
